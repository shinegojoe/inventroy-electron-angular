import { BaseDao } from '../../electron/dao/baseDao';
import { IFieldSearch } from '../../app/shared/plyload';
import { checkIsSameKey } from '../utils';
import { Item } from '../../app/shared/model';


// test("listByField<T>, should return T[]", async()=> {
//   const tableName = "item";
//   const addSql = "";
//   const updateSql = "";
//   const baseDao = new BaseDao(tableName, addSql, updateSql);
//   const payload: IFieldSearch = {
//     field: "name",
//     val: "床aaa"
//   }
//   const res = await baseDao.listByField<Item>(payload);
//   console.log("res: listByField", res);
//   expect(Array.isArray(res)).toBe(true);
//   const newItem = new Item();
//   checkIsSameKey(res, newItem);

// })
test("", ()=> {
  
})
